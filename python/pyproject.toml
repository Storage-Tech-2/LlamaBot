[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.128.0",
  "pydantic>=2.12.5",
  "sentence-transformers>=5.2.2",
  "uvicorn>=0.40.0",
]
